# audio_cli (Windows)

这个目录用于 Windows 端命令行程序（CLI）。

当前状态：目录骨架占位。
建议后续结构：
- `windows/src`：Windows CLI 源码
- `windows/cmake`：Windows CLI 构建脚本

版本说明：
- 表现层版本：`0.1.0`
- 内核版本：运行 `binary_audio_cpp version` 自动读取 `core` 版本并显示。
