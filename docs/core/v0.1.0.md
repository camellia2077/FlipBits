# Core v0.1.0 更新说明

发布日期：2026-03-01

## 版本信息
- 内核版本：`0.1.0`
- 适用模块：`apps/audio_core`

## 变更内容
- 固化内核版本接口：
  - C++ 接口：`bag::CoreVersion()`
  - C API 接口：`bag_core_version()`
- 版本字符串由内核统一维护，对上层（CLI/Android）提供只读查询能力。

## 兼容性
- 该版本不引入协议格式变更。
- 现有 `encode/decode` 行为保持兼容。
